# SIP Website Environment Configuration
# Copy this to .env.local for local development

# Enable real swaps via NEAR 1Click API (default: false = demo mode)
# NEXT_PUBLIC_REAL_SWAPS=true

# Optional: NEAR Intents JWT token for authenticated API access
# Get one at https://near-intents.org
# NEXT_PUBLIC_NEAR_INTENTS_JWT=your-jwt-token-here

# ─── Zcash RPC Configuration ─────────────────────────────────────────────────────
# Configure these to enable live Zcash data in the SDK Showcase.
# Without these, the showcase runs in demo mode with sample data.
#
# For testnet, run zcashd with: zcashd -testnet -rpcuser=user -rpcpassword=pass
# Default ports: 8232 (mainnet), 18232 (testnet)

# ZCASH_RPC_HOST=127.0.0.1
# ZCASH_RPC_PORT=18232
# ZCASH_RPC_USER=your-rpc-username
# ZCASH_RPC_PASS=your-rpc-password
# ZCASH_RPC_TESTNET=true

# ─── Sentry Error Monitoring ──────────────────────────────────────────────────
# Optional: Enable error tracking and performance monitoring with Sentry
# Get your DSN from https://sentry.io/settings/projects/
#
# Required for error tracking:
# NEXT_PUBLIC_SENTRY_DSN=https://your-project-key@o123456.ingest.sentry.io/7890123
#
# Optional: For source map uploads (CI/CD only)
# SENTRY_AUTH_TOKEN=your-auth-token
# SENTRY_ORG=your-org-slug
# SENTRY_PROJECT=sip-website
